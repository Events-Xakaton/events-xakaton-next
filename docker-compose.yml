services:
  frontend:
    build:
      context: .
      args:
        # NEXT_PUBLIC_* встраивается в JS при сборке — браузер обращается напрямую к хосту
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-http://localhost:4000/api}
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    restart: unless-stopped
